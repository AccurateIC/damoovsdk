<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- Input for Phone -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/identifierEdtLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:boxCornerRadiusBottomEnd="30dp"
        app:boxCornerRadiusBottomStart="30dp"
        app:boxCornerRadiusTopEnd="30dp"
        app:boxCornerRadiusTopStart="30dp"
        app:boxBackgroundMode="outline"
        app:boxStrokeColor="@color/colorPrimary"
        android:layout_marginTop="16dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/identifierEdt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter phone number"
            android:textColor="#000000"
            android:background="@drawable/bg_card"
            android:padding="20dp"
            android:inputType="number"
            android:textColorHint="#898989"
            android:gravity="center_vertical"
            android:textSize="16sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Optional Name input (for registration) -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/nameEdtLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:boxCornerRadiusBottomEnd="30dp"
        app:boxCornerRadiusBottomStart="30dp"
        app:boxCornerRadiusTopEnd="30dp"
        app:boxCornerRadiusTopStart="30dp"
        app:boxBackgroundMode="outline"
        app:boxStrokeColor="@color/colorPrimary"
        android:layout_marginTop="16dp">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/nameEdt"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your name (optional)"
            android:textColor="#000000"
            android:background="@drawable/bg_card"
            android:padding="20dp"
            android:textColorHint="#898989"
            android:gravity="center_vertical"
            android:textSize="16sp" />
    </com.google.android.material.textfield.TextInputLayout>

    <!-- Send OTP Button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/sendOtpBtn"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:text="Send OTP"
        android:backgroundTint="@color/colorPrimary"
        android:textColor="@color/white"
        app:cornerRadius="30dp"
        android:layout_marginTop="20dp" />

    <!-- Login with Email Option -->
    <TextView
        android:id="@+id/loginWithEmailTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Already registered with email?"
        android:textColor="#898989"
        android:textSize="14sp"
        android:layout_marginTop="24dp"
        android:layout_gravity="center_horizontal" />

    <TextView
        android:id="@+id/loginWithEmailActionTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Login with Email Instead"
        android:textColor="@color/colorPrimary"
        android:textSize="16sp"
        android:textStyle="bold"
        android:layout_marginTop="6dp"
        android:layout_gravity="center_horizontal" />

</LinearLayout>
